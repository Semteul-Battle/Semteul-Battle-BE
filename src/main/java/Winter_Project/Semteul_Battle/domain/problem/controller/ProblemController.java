package Winter_Project.Semteul_Battle.domain.problem.controller;

import Winter_Project.Semteul_Battle.global.security.jwt.JwtTokenProvider;
import Winter_Project.Semteul_Battle.domain.problem.dto.*;
import Winter_Project.Semteul_Battle.domain.contest.repository.ContestRepository;
import Winter_Project.Semteul_Battle.domain.problem.repository.IORepository;
import Winter_Project.Semteul_Battle.domain.problem.repository.ProblemRepository;
import Winter_Project.Semteul_Battle.domain.contest.service.ContestLiveService;
import Winter_Project.Semteul_Battle.domain.problem.service.*;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.util.List;

@RestController
@RequiredArgsConstructor
@Slf4j
@RequestMapping("/contests")
public class ProblemController {
    private final AddProblemService addProblemService;
    private final JwtTokenProvider  jwtTokenProvider;
    private final DeleteProblemService deleteProblemService;
    private final DeleteIOService deleteIOService;
    private final ProblemRepository problemRepository;
    private final ContestRepository contestRepository;
    private final ContestLiveService contestLiveService;
    private final UpdateProblemService updateProblemService;
    private final UpdateIOService updateIOService;
    private final IORepository ioRepository;
    private final AddIOService addIOService;
    private final UploadPictureService uploadPictureService;

    // ?????怨룹씐?????嶺?????????댄뱼??
@PostMapping("/addProblem")
    public boolean addProblem(@RequestBody AddProblemDto addProblemDto,
                              @RequestHeader("Authorization") String token) {
        Long contestId = addProblemDto.getContestId();
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
if (participantStatus == 0) { // ???????????? ?饔낅떽????????棺堉?뤃????            addProblemService.problemFrame(addProblemDto);
return true;
        } else { // ???????????? ?????諛몃마?????棺堉?뤃????
return false;
        }
    }
    //?????怨룹씐????????살퓢???????댄뱼??
@PostMapping("/addIO")
    public boolean addIO(@RequestBody AddIODto addIODto,
                         @RequestHeader("Authorization") String token) {
        Long contestId = addIODto.getContestId();
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
if (participantStatus == 0) { // ???????????? ?饔낅떽????????棺堉?뤃????            addIOService.IOFrame(addIODto);
return true;
        } else { // ???????????? ?????諛몃마?????棺堉?뤃????
return false;
        }
    }

    // ???嶺????????
@DeleteMapping("/deleteProblem")
    public boolean deleteProblem(@RequestBody DeleteProblemDto deleteProblemDto,
                                 @RequestHeader("Authorization") String token) {
        Long contestId = deleteProblemDto.getContestId();
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
        // ?????????????? ??轅붽틓????????
if (participantStatus == 0) {
            // ?????????? ???嶺???傭?끆??????? ???怨쀫뮡??????汝뷴젆??녷뉩??읂?γ볥덆???? ??轅붽틓????????
boolean contestExists = contestRepository.existsById(contestId);
            boolean problemExists = problemRepository.existsById(deleteProblemDto.getProblemId());
            boolean ioExists = ioRepository.existsById(deleteProblemDto.getIoId());
            // ????? ???嶺???傭?끆??????? ?饔낅떽????ш낄?뉔뇡??????怨쀫뮡??????汝뷴젆??녷뉩??읂???棺堉?뤃???????????嶺???????????꿔꺂?€??洹?즶????轅붽틓?????
if (contestExists && problemExists && ioExists) {
                deleteIOService.deleteIO(deleteProblemDto);
                deleteProblemService.deleteProblem(deleteProblemDto);
                return true;
            } else {
                // ????????????嶺???傭?끆??????? ???怨쀫뮡?????? ??????猿딅빟 ??棺堉?뤃????
return false;
            }
        } else { // ???????????? ?????諛몃마?????棺堉?뤃????
return false;
        }
    }

    // ???嶺????????癰궽블뀯??
@PatchMapping("/updateProblem")
    public boolean updateProblem(@RequestBody UpdateProblemDto updateProblemDto,
                                 @RequestHeader("Authorization") String token) {
        Long contestId = updateProblemDto.getContestId();
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
        // ?????????????? ??轅붽틓????????
if (participantStatus == 0) {
            // ?????????? ???嶺???傭?끆??????? ???怨쀫뮡??????汝뷴젆??녷뉩??읂?γ볥덆???? ??轅붽틓????????
boolean contestExists = contestRepository.existsById(contestId);
            boolean problemExists = problemRepository.existsById(updateProblemDto.getProblemId());
            boolean ioExists = ioRepository.existsById(updateProblemDto.getIoId());
            // ???? ???嶺??????? ??轅붽틓?????????????嶺??????????⑤뜤??????
if (contestExists && problemExists && ioExists) {
                updateProblemService.updateProblem(updateProblemDto);
                updateIOService.UpdateIO(updateProblemDto);
                return true;
            } else {
                return false;
            }
        } else {
            return false;
        }
    }

    // ????????熬곣뫖利???
@PostMapping("/addFile")
    public ResponseEntity<String> generateFiles(@RequestBody AddIOFileDto addIOFileDto,
                                                @RequestHeader("Authorization") String token) {
        Long contestId = addIOFileDto.getContestId();
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
if (participantStatus == 0) { // ???????????? ?饔낅떽????????棺堉?뤃????            // ??????ㅼ굣塋?????????????????????熬곣뫖利???            addProblemService.createInputOutputFiles(addIOFileDto.getInputFile(), addIOFileDto.getOutputFile(), addIOFileDto.getNumber());
            return ResponseEntity.ok("????????熬곣뫖利?????????諛몃마??????????"); // ????????熬곣뫖利????????諛몃마???饔낅떽????????? ????썹땟戮녹????
} else { // ???????????? ?????諛몃마?????棺堉?뤃????
return ResponseEntity.status(HttpStatus.FORBIDDEN).body("??????????????깅즽????????놁졄."); // ?????????????⑤뜤???????????썹땟戮녹????
}
    }


    // ???嶺????????????????嶺뚮ㅎ?쀧댆?
@PostMapping("/uploadPictures")
    public ResponseEntity<List<String>> uploadPictures(@RequestHeader("Authorization") String token,
                                                       @RequestParam("contestId") Long contestId,
                                                       @RequestParam("problemId") Long problemId,
                                                       @RequestPart("files") List<MultipartFile> files) {
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
if (participantStatus == 0) { // ????????????????棺堉?뤃????
try {
List<String> imageUrls = uploadPictureService.uploadPictures(files, problemId);
                return ResponseEntity.ok(imageUrls);
            } catch (IOException e) {
                log.error("Failed to upload pictures: {}", e.getMessage());
                return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(null);
            }
        } else { // ???????????? ?????諛몃마?????棺堉?뤃????
return ResponseEntity.status(HttpStatus.FORBIDDEN).body(null);
        }
    }

    // ???嶺????????????癰궽블뀯??
@PatchMapping("/updatePictures")
    public ResponseEntity<List<String>> updatePictures(@RequestHeader("Authorization") String token,
                                                       @RequestParam("problemId") Long problemId,
                                                       @RequestParam("contestId") Long contestId,
                                                       @RequestPart("files") List<MultipartFile> files) {
        String tokenFromId = jwtTokenProvider.extractLoginIdFromToken(token); // ????影?력??????id ?????댄뱼???
Long participantStatus = contestLiveService.whoAreU(contestId, tokenFromId); // ?饔낅떽????怨뚮옩?????/ ??????????????????
if (participantStatus == 0) { // ????????????????棺堉?뤃????
try {
List<String> updatedImageUrls = uploadPictureService.updatePictures(files, problemId);
                if (updatedImageUrls != null && !updatedImageUrls.isEmpty()) {
                    return ResponseEntity.ok(updatedImageUrls);
                } else {
                    // ????癰궽블뀯????????⑤슣??
return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(null);
                }
            } catch (IOException e) {
                log.error("Failed to update pictures: {}", e.getMessage());
                return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR).body(null);
            }
        } else { // ???????????? ?????諛몃마?????棺堉?뤃????
return ResponseEntity.status(HttpStatus.FORBIDDEN).body(null);
        }
    }
}